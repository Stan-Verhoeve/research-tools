[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "research_tools"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["matplotlib"]

[tool.setuptools.packages.find]
include = ["research_tools*"]
